export type { InventoryProduct, SiteInventory, InventorySnapshot } from "../shared/inventory";

export interface BrandDefinition {
  name: string;
  shortMark: string;
  url: string;
  themeClass: string;
}
